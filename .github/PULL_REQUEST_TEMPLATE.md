<!-- Keep the headings; the comments are hints, delete them. Every sentence
     of the description that states a behaviour — of the image, of a script,
     of a tool — goes under exactly one of the three headings below. -->

## What changes

<!-- One paragraph. Under Dockerfile, .dockerignore, entrypoint.sh or
     flyway-callbacks/? Then say so: merging does not ship it, moving
     `release` does. -->

Closes #

## Measured

<!-- What was run, on what — a published tag or steve:local, which
     architecture, emulated or native — and the output that backs each claim.
     A Dockerfile or workflow change without a build, a label change without
     .Config.Labels, a hack/ script without its suite: not measured. -->

## Documented elsewhere

<!-- Behaviour taken from a tool's own documentation and not re-run here,
     and whose documentation. Two comments in this repository were written
     from plausible reasoning and turned out false when measured. -->

## Not verified

<!-- What was not run, and why — a restart of the shared daemon, a product
     nobody has, a path only the next release exercises. Leaving this empty
     is a claim too. -->
