---
# Build & push the SteVe image (OCPP CSMS) to GHCR.
#
# Runs on GitHub-hosted runners, never on a self-hosted one: this repository is
# public, so a pull request from a fork would otherwise execute arbitrary code
# on our own machine. Hosted runners are free and unlimited on public repos.
#
# SteVe runs its jOOQ code generation and its Flyway migrations against a LIVE
# database during `mvn package`. We therefore start a throwaway MariaDB that
# publishes 3306 on the runner, and build with `--network=host` so the RUN steps
# share the runner's network namespace and reach it on 127.0.0.1.
#
# Why not a dedicated Docker network: BuildKit only accepts host/none/default
# for `--network`. The legacy builder (DOCKER_BUILDKIT=0) did accept a custom
# network, but it has been deprecated since Engine 23 — we stay on the supported
# path. See the README for the exact same commands, runnable locally.
#
# No secret ever enters the image: sensitive configuration is injected at
# runtime through environment variables (Spring externalized config).

name: Build SteVe image

on:
  workflow_dispatch:
    inputs:
      steve_ref:
        description: "SteVe release tag to build (e.g. steve-3.13.0)"
        required: true
        # renovate: datasource=github-releases depName=steve-community/steve
        default: "steve-3.13.0"
  # No `push:` trigger. Publishing used to happen on every merge touching the
  # packaging, which conflated two different events: "the packaging changed" and
  # "a release should go out". A typo fixed in a comment here republished
  # steve-X.Y.Z under a new digest, so the tag moved without anything about the
  # image having changed. Releasing is now the deliberate act above, and
  # `release-drift.yml` watches for the failure mode that trade introduces —
  # a packaging change merged and then forgotten.
  #
  # Pull requests still run the whole build, minus the login and the push: it is
  # the only thing standing between an unbuildable change and `main`.
  pull_request:
    paths:
      - "Dockerfile"
      - ".dockerignore"
      - "entrypoint.sh"
      - "flyway-callbacks/**"
      - ".github/workflows/build-image.yml"

permissions:
  contents: read
  packages: write

concurrency:
  # Releases queue rather than cancel each other; pull request runs supersede
  # their own previous run.
  group: steve-image-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  IMAGE: ghcr.io/juherr/steve
  # Throwaway MariaDB for the code generation (same defaults as
  # application-docker.properties). Its credentials only serve the build: no
  # data, no secret, nothing of it reaches the image.
  # renovate: datasource=docker depName=mariadb
  BUILD_DB_IMAGE: "mariadb:11.8"

jobs:
  build:
    name: build & push
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      # The tag comes from user input and is interpolated into shell commands
      # below, so validate it against a strict format BEFORE any use. The
      # validation also covers the pull-request path, where the value is read
      # out of the Dockerfile rather than typed.
      - name: Resolve and validate the SteVe tag
        id: ref
        env:
          INPUT_REF: ${{ inputs.steve_ref }}
        run: |
          set -euo pipefail
          ref="${INPUT_REF:-}"
          if [ -z "$ref" ]; then
            # Pull request: no input to read. Take the Dockerfile's own default
            # rather than keeping a second copy of the release here — it is the
            # version this branch would actually ship.
            ref=$(sed -n 's/^ARG STEVE_REF=//p' Dockerfile | head -1)
          fi
          if ! printf '%s' "$ref" | grep -Eq '^steve-[0-9]+\.[0-9]+\.[0-9]+$'; then
            echo "Invalid SteVe tag: '$ref' (expected steve-X.Y.Z)"; exit 1
          fi
          echo "steve_ref=$ref" >> "$GITHUB_OUTPUT"

      # Published on 0.0.0.0 rather than the loopback so that the documented
      # fallback (`--add-host …:host-gateway`, no host networking) also works
      # without touching this step. The runner is ephemeral and firewalled.
      - name: Start the throwaway MariaDB (jOOQ/Flyway code generation)
        run: |
          set -euo pipefail
          docker run -d --name steve-build-db \
            -p 3306:3306 \
            -e MARIADB_ROOT_PASSWORD=root \
            -e MARIADB_DATABASE=stevedb \
            -e MARIADB_USER=steve \
            -e MARIADB_PASSWORD=changeme \
            -e TZ="+00:00" \
            "${BUILD_DB_IMAGE}" --innodb-use-native-aio=0

      - name: Wait for the database to be ready
        run: |
          set -euo pipefail
          for i in $(seq 1 60); do
            if docker exec steve-build-db \
                 healthcheck.sh --connect --innodb_initialized; then
              echo "Database ready."; exit 0
            fi
            echo "Database not ready yet ($i/60)…"; sleep 2
          done
          echo "Timeout: the build database never became ready."; exit 1

      # Fails in seconds with a clear message instead of after eight minutes of
      # Maven, should the build sandbox not reach the database.
      - name: Probe build sandbox networking
        run: |
          set -euo pipefail
          printf 'FROM alpine\nRUN nc -z -w3 127.0.0.1 3306 && echo REACHABLE\n' \
            | docker build --network=host --progress=plain -

      - name: Build the image
        env:
          STEVE_REF: ${{ steps.ref.outputs.steve_ref }}
        run: |
          set -euo pipefail
          # 127.0.0.1 and not localhost: in the host network namespace localhost
          # may resolve to ::1 first, while the published MariaDB listens on IPv4.
          docker build \
            --network=host \
            --build-arg STEVE_REF="${STEVE_REF}" \
            --build-arg DB_IP=127.0.0.1 \
            --build-arg BUILD_DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
            --build-arg VCS_REF="${GITHUB_SHA}" \
            -t "${IMAGE}:${STEVE_REF}" \
            .

      # The recommended Compose healthcheck shells out to curl inside the
      # container; make sure the runtime stage really ships it.
      - name: Validate healthcheck dependency
        env:
          STEVE_REF: ${{ steps.ref.outputs.steve_ref }}
        run: |
          set -euo pipefail
          docker run --rm --entrypoint curl "${IMAGE}:${STEVE_REF}" --version >/dev/null

      - name: Login to GHCR
        if: github.event_name == 'workflow_dispatch'
        env:
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GHCR_USER: ${{ github.actor }}
        run: echo "${GHCR_TOKEN}" | docker login ghcr.io -u "${GHCR_USER}" --password-stdin

      # One tag per SteVe release, and nothing else. The JRE is a build detail
      # recorded in the image (see the README), not part of the tag: a JRE bump
      # is not a new SteVe release and must not fragment the tag namespace.
      #
      # No `latest`, and no `sha-${GITHUB_SHA}` tag: the latter WOULD add a
      # stored version per build, for no benefit — consumers pin by digest.
      - name: Push
        if: github.event_name == 'workflow_dispatch'
        env:
          STEVE_REF: ${{ steps.ref.outputs.steve_ref }}
        run: |
          set -euo pipefail
          docker push "${IMAGE}:${STEVE_REF}"
          echo "Pushed: ${IMAGE}:${STEVE_REF}"
          echo "Digest to pin:"
          docker inspect --format '{{ index .RepoDigests 0 }}' "${IMAGE}:${STEVE_REF}"

      - name: Cleanup
        if: always()
        run: |
          docker rm -f steve-build-db || true
          docker logout ghcr.io || true
